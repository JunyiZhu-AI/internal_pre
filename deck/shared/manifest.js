/* Page order for the deck. deck.js reads this for prev/next navigation
   and the "N / M" counter. Add new pages here, in presentation order.

   NOTE: slide 01 currently has three candidate variants (a/b/c) in the
   manifest so the arrow keys can flip between them for comparison.
   After one is chosen it becomes the single 01 page.
   (01-qkt-matmul.html is the engine template page, kept out of the flow.) */
window.DECK_PAGES = [
  "01a-pricing-receipts.html",
  "01b-pricing-equation.html",
  "01c-pricing-ledger.html",
];
